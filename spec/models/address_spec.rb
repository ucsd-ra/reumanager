require 'spec_helper'

describe Address do
#  it "is valid with an address, address2, city, country, state, zip, label, permanent" do
#    address = Address.new(city: 'San Diego', country: 'United States of America', state: 'CA', zip:'92092', label: 'yes', permanent: 'unsure')
#    expect(address).to be_valid
#  end
  
#  it "is invalid without a city" do
#    expect(Address.new(city: nil)).to have(1).error_on(:city)
#  end
  
#  it "is invalid without a zip" do
#    expect(Address.new(zip: nil)).to have(1).error_on(:zip)
#  end
  
#  it "is invalid without a label" do
#    expect(Address.new(label: nil)).to have(1).error_on(:label)
#  end
  
#  it "is invalid without a permanent" do
#    expect(Address.new(permanent: nil)).to have(1).error_on(:permanent)
#  end
  
end
