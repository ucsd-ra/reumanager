<div id="header">

	<h1><%= Setting.app_title %></h1>
	<h3><%= Time.now.year %> Program Dates: <%= "#{Setting.program_start_date.to_date.strftime("%B	 %d")} - #{Setting.program_end_date.to_date.strftime("%B %d")}" %></h3>
	
	<%= render :partial => "layouts/buttons" %>
</div>