<div class="span-13 push-5 bordered colored padded rounded">
	<% if @user.submitted_at -%>
		<p>Your application was successfully submitted on <%= @user.submitted_at.strftime("%B %d at %I:%M %p") %>.</p>
	<% else -%>
		<% if Time.now > DateTime.new(2011,3,6,0,0) %>
			<p>Your application was not submitted because the deadline has past.</p>
		<% else -%>
			<p>Your application was not submitted because the deadline has past.</p>
		<% end -%>
	<% end -%>

	<% if @recommendation -%>
		<p>Your recommendation was successfully submitted by <%= @recommender.name %> (&nbsp;<%= mail_to @recommender.email, @recommender.email %>&nbsp;) on <%= @recommendation.created_at.strftime("%B %d at %I:%M %p") %>.</p>
	<% else -%>
		<% if @rec_request_at && @recommender -%>
			<p>We have not received a recommendation yet. Your request was previously sent on <%= @rec_request_at.strftime("%B %d at %I:%M %p") %> to <%= @recommender.name %> at <%= @recommender.email %>.  We will continue to take recommendations after the application deadline.</p>
		<% end -%>
	<% end -%>

	<% unless @user.completed_at == nil -%>
		<h5>Congratulations, your application is complete!</h5>
	<% end %>

	<%= link_to "Application Status", { :controller => "users", :action => "status" }, :class => "button" unless params[:action] == "status" %>

	<% unless @user.completed_at != nil || @recommendation -%>
		<% if @user.rec_request_at -%>
			<%= link_to "Resend Recommendation Request", { :controller => "users", :action => "resend_request" }, :class => "button", :onclick => "$('submit').hide();$('overlay').show();$('opaque_overlay').show();$('wait_box').show();" %>
		<% else -%>
			<%= link_to "Send Recommendation Request",  { :controller => "users", :action => "resend_request" }, :class => "button", :onclick => "$('submit').hide();$('overlay').show();$('opaque_overlay').show();$('wait_box').show();" %>
		<% end -%>
	<% end -%>	
</div>
